# Context Chat Frontend

Frontend.

## Setup

Create `frontend/.env`:

```bash
BACKEND_API_URL="http://127.0.0.1:8000"
```

Install and run:

```bash
bun install
bun dev
```

Open `http://localhost:3000`.

## Scripts

```bash
bun dev
bun run build
bun run lint
```
